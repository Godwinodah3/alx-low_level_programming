#include "main.h"

/**
 * _strcat - concatinating a string
 *
 * @dest: value used
 * @src: vale used
 *
 *Return: dest
 */

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
