/**
 * main - check the code
 *
 * Return: Always 0.
 */
void _puts(char *str);
int main(void)

{
    char *str;
    int i;

    str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
    _putchar(str[i]);
    return (0);
}

